.resource-bar {
  margin-bottom: 4rpx;
}

.resource-bar.small {
  font-size: 20rpx;
}

.resource-bar.small .pixel-progress {
  height: 10rpx;
}

.resource-header {
  margin-bottom: 2rpx;
}

.resource-label {
  gap: 6rpx;
}

.resource-icon {
  font-size: 24rpx;
}

.small .resource-icon {
  font-size: 18rpx;
}

.resource-name {
  color: var(--color-text);
  font-family: monospace;
  -webkit-font-smoothing: none;
  font-size: 24rpx;
}

.small .resource-name {
  font-size: 20rpx;
}

.resource-info {
  font-family: monospace;
  -webkit-font-smoothing: none;
}

.resource-value {
  color: var(--color-text);
  font-weight: bold;
  font-size: 24rpx;
}

.small .resource-value {
  font-size: 20rpx;
}

.resource-max {
  color: var(--color-text-dim);
  font-size: 20rpx;
}

.small .resource-max {
  font-size: 18rpx;
}

.resource-percent {
  color: var(--color-text-dim);
  font-size: 18rpx;
  margin-left: 4rpx;
}

.small .resource-percent {
  font-size: 16rpx;
}

.pixel-progress {
  height: 14rpx;
  background: #0a0a14;
  border: 1rpx solid #333;
  position: relative;
  overflow: hidden;
}

.pixel-progress-fill {
  height: 100%;
  transition: width 0.4s steps(10);
}

.pixel-progress-fill.danger {
  animation: dangerPulse 0.5s steps(2) infinite;
}

@keyframes dangerPulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.5; }
}

.recent-delta {
  font-size: 20rpx;
  font-family: monospace;
  text-align: right;
  margin-top: 2rpx;
}
